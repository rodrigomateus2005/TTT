import { DevExtremeGridViewDirective } from './dev-extreme-grid-view.directive';

describe('DevExtremeGridViewDirective', () => {
  it('should create an instance', () => {
    const directive = new DevExtremeGridViewDirective();
    expect(directive).toBeTruthy();
  });
});
