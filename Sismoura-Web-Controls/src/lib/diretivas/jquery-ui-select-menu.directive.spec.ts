import { JqueryUiSelectMenuDirective } from './jquery-ui-select-menu.directive';

describe('JqueryUiSelectMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new JqueryUiSelectMenuDirective();
    expect(directive).toBeTruthy();
  });
});
