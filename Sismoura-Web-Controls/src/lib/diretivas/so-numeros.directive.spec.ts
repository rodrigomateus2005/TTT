import { SoNumerosDirective } from './so-numeros.directive';

describe('SoNumerosDirective', () => {
  it('should create an instance', () => {
    const directive = new SoNumerosDirective();
    expect(directive).toBeTruthy();
  });
});
